services:
  receive:
    build: "receive/"
    #ports:
    #  - "8001:8001"
    volumes:
      - thread-art-vol:/app/thread-art-vol
  act:
    build: "act/"
    volumes:
      - thread-art-vol:/app/thread-art-vol
  server:
    image: caddy:2.6
    ports:
      - "1025:80"
      #- "80:80"
      #- "443:443"
      #- "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:Z
      - ./frontend:/srv:Z
      - caddy_data:/data
      - caddy_config:/config
volumes:
  thread-art-vol:
